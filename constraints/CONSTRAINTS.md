# 板级 XDC 约束

本目录用于存放上板工程的板级约束文件。实际开发板型号确认后，使用以下结构：

```text
constraints/<board-model>/<board-model>.xdc
```

优先使用开发板厂商提供、且与实际板卡修订版本匹配的 XDC。首版 ILA 工程至少需要板载时钟的管脚、电平标准和 `create_clock` 约束；复位及其他外设端口按实际使用情况补充。

当前尚未确认开发板型号，因此本目录不含可用于实现的 XDC 文件。
